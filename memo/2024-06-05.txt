애니메이션
    transform(변형)-rotate(), trans
    transition(시간)
    timing-funtion()

가상 선택자 - 콜론(:) 접두어인(프리픽스)인것
    :hover
    :link
    :focus

순서를 지정
    p:nth-of-type(2n) - p요소 중에서 n번째 요소

    h:nth-child(2n) - 부모 요소 안에 있는 모든 요소 중에서 n번째 자식 요소 선택

    p::before
    li::after


@Keyframes 이름 {}
